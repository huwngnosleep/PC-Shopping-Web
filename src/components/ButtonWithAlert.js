
import React, { useState, useEffect } from 'react'

const ButtonWithAlert = (props) => {
    return(
        
    )
}

const styles = {
    button: {
        position: 'relative',
    },
    alert: {
        position: 'absolute',
        top: '10px',
        right: '10px',
        display: 'block',
    }
}

export default ButtonWithAlert